import React from "react";
import ReactDOM from "react-dom";
import { App, GlobalStyle } from "./component";

ReactDOM.render(
  <>
    <GlobalStyle />
    <App />
  </>,
  document.getElementById("root")
);
